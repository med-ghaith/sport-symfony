{% extends 'back.base.html.twig' %}

{% block body %}

    {{ include('event/_form.html.twig', {'button_label': 'Update'}) }}

    <button style="background-color: orange">  <a href="{{ path('event_index') }}">Back to list</a> </button>

{% endblock %}
